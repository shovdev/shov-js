{
  "name": "shov-js",
  "version": "2.0.0",
  "description": "JavaScript SDK for Shov - AI Database Engine with vector search and real-time streaming",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rm -rf dist && esbuild src/index.ts --bundle --platform=node --packages=external --external:ws --outfile=dist/index.js && esbuild src/index.ts --bundle --format=esm --packages=external --external:ws --outfile=dist/index.mjs && tsc --emitDeclarationOnly --outDir dist",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "database",
    "key-value",
    "vector",
    "ai",
    "edge",
    "serverless",
    "sdk",
    "storage",
    "cloudflare",
    "javascript",
    "typescript"
  ],
  "author": "Shov",
  "license": "MIT",
  "homepage": "https://shov.com",
  "repository": {
    "type": "git",
    "url": "https://github.com/shovdev/shov-js.git"
  },
  "bugs": {
    "url": "https://github.com/shovdev/shov-js/issues"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/ws": "^8.5.0",
    "esbuild": "^0.20.2",
    "typescript": "^5.3.0"
  },
  "optionalDependencies": {
    "ws": "^8.16.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "publishConfig": {
    "access": "public"
  }
}
