{
  "name": "shov-sdk",
  "version": "1.0.0",
  "description": "JavaScript SDK for Shov - The AI Memory Engine",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rm -rf dist && esbuild src/index.ts --bundle --platform=node --packages=external --outfile=dist/index.js && esbuild src/index.ts --bundle --format=esm --packages=external --outfile=dist/index.mjs && tsc --emitDeclarationOnly --outDir dist",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "database",
    "key-value",
    "vector",
    "ai",
    "edge",
    "serverless",
    "sdk",
    "storage",
    "cloudflare",
    "javascript",
    "typescript"
  ],
  "author": "Shov",
  "license": "MIT",
  "homepage": "https://shov.com",
  "repository": {
    "type": "git",
    "url": "https://github.com/shovdev/shov.git",
    "directory": "packages/shov-sdk"
  },
  "bugs": {
    "url": "https://github.com/shovdev/shov/issues"
  },
  "devDependencies": {
    "esbuild": "^0.20.2",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "files": [
    "dist",
    "README.md"
  ]
}
